[package]
name = "api-rust-rocket"
version = "0.1.0"
edition = "2021"


[dependencies]
rocket = { version = "=0.5.0-rc.3", features = ["json"] }
sqlx = { version = "0.7.1", features = ["postgres", "time", "uuid", "macros"] }
